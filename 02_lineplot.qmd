---
title: "02_lineplot"
author: "John Little"
format: html
---

```{ojs}
// newdata = FileAttachment("data/starsmall_freqtbl.csv").csv({ typed: true})
newdata = FileAttachment("worshops_over_time.csv").csv({ typed: true})
```

```{ojs}
newdata.columns

```

```{ojs}
// 'workshop_name', 'workshop_id', 'workshop_date'
Inputs.table(newdata, {sort: 'workshop_date', reverse: true, rows: 7})
```

```{ojs}
viewof search = Inputs.search(newdata)
```

```{ojs}
Inputs.table(search)
```

```{ojs}
Plot.plot({
  marks: [
    Plot.line(search, {x: 'workshop_date', y: 'n'})
]
})
```

```{ojs}
viewof table = Inputs.table(newdata)
```

no chart = 

```{ojs}
search.view()
```


```{ojs}
LineChart(search, {
  x: d => d.workshop_date,
  y: d => d.n,
  yLabel: "Number of Registrants",
  width: 800,
  height: 400,
  color: "darkorange"
})
```

```{ojs}
chart = LineChart(search, {
  x: d => d.workshop_date,
  y: d => d.n,
  yLabel: "Number of Registrants",
  width: 800,
  height: 400,
  color: "darkorange"
})
```

```{ojs}
chartinline = InlineChart(search, {
  x: d => d.workshop_date,
  y: d => d.n,
//  z: d => d.fruit,
  width: 800,
  height: 500
})
```

```{ojs}
percent_chart = ChangeLineChart(search, {
  x: d => d.workshop_date,
  y: d => d.n,
  // basis, // e.g., 93.24 (omit for the first y-value)
  yLabel: "↑ Change in attendance (%)",
  width: 800,
  height: 500,
  color: "steelblue"
})
```


***

```{ojs}
viewof island_selected = Inputs.select(  
  ["Torgersen", "Biscoe", "Dream"],
  { // value: ["Torgersen", "Biscoe"],
    label: "Islands:"
  })
  
viewof species_selected = Inputs.select(  
  ["Adelie", "Gentoo", "Chinstrap"],
  { label: "Species:" }
  )

island_selected
species_selected
```

```{ojs}
// viewof search = Inputs.search(newdata)
// Inputs.table(search)
viewof year_df = Inputs.checkbox(["salty", "sweet", "bitter", "sour", "umami"], {label: "Flavors"})

viewof r = Inputs.radio(
  ["Lust", "Gluttony", "Greed", "Sloth", "Wrath", "Envy", "Pride"]
  )
  
viewof color = Inputs.radio(["red", "green", "blue"], {label: "color"})

viewof dd1 = Inputs.select({
  title: "Stooges",
  description: "Please pick your favorite stooge.",
  options: ["Curly", "Larry", "Moe", "Shemp"],
  value: "Moe"
})

r

color

dd1
```



***

https://observablehq.com/@d3/line-chart https://observablehq.com/@d3/inline-labels?collection=@d3/charts

```{ojs}
// import { LineChart }  from "@d3/line-chart"
import { LineChart } from "@d3/line-with-tooltip"
import {InlineChart} from "@d3/inline-labels"
import { ChangeLineChart } from "@d3/change-line-chart"
```
